#include <iostream>
#include <vector>

using namespace std;

vector<int> v1,v2,v3,v4;

int main() {
    v1.resize(8);
    v2.resize(8);
    v3.resize(8);
    v4.resize(8);

    for(int i = 0 ; i < 8; ++i){
        cin >> v1.[i];
    }
        for(int i = 0 ; i < 8; ++i){
        cin >> v1.[i];
    }
    return 0;
}